sudo apt update
./autorun.sh install


sudo apt installa git
sudo apt update


python -m pip install --upgrade pip
pip install setuptools setuptools_scm_git_archive

pip install setuptools==58.0.0


sudo apt install python3-pip
sudo ln -s /usr/bin/python3 /usr/bin/python

sudo apt install python3-dev
buildozer android clean
buildozer android debug


# The Android archs to build for, choices: armeabi-v7a, arm64-v8a, x86, x86_64
# In the past, was `android.arch` as we weren't supporting builds for multiple archs at the same time.
android.archs = arm64-v8a

https://buildozer.readthedocs.io/en/stable/quickstart.html
